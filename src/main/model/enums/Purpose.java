package model.enums;

// Represents a list of all the purposes that can be chosen while creating an organization
// Purpose can further be used to filter out the results while searching for organizations to apply for by an applicant

public enum Purpose {
    HEALTH, EDUCATION, UNDERPRIVILEGED, ANIMALS
}
